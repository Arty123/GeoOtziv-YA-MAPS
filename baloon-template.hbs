<div class="popup" id="popup">
    <div class="popup__header">
        <span class="popup__header-inner">
            <i class="fa fa-map-marker popup__header-marker" aria-hidden="true"></i>
            <span class="popup__header-text">{{ address }}</span>
        </span>
    </div>
    <div class="popup__main">
        <div class="popup__main-reviews" id="reviews">
            <ul class="review__list" id="reviews-list">
            {{#each lastReviews}}
                <li class="review__item">
                    <div class="review__list-name">{{ name }} <span class="review__list-content">{{ date }}</span></div>
                    <div class="review__list-content">{{ review }}</div>
                </li>
            {{/each}}
            </ul>
        </div>
        <div class="popup__main-add clearfix">
            <div class="popup__main-add-header">ВАШ ОТЗЫВ</div>
            <form name="form" method="POST">
                <input type="text" id="inputName" name="name" placeholder="Ваше имя" class="popup__input">
                <input type="text" id="inputPlace" name="place" placeholder="Укажите место" class="popup__input">
                <textarea name="message" id="inputReview" placeholder="Поделитесь впечатлениями" class="popup__textarea"></textarea>
                <a data-add="true" class="save__btn" href="#" id="btn">Добавить</a>
            </form>
        </div>
    </div>
</div>